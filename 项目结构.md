E:\desktop\yolo-lstm-akf\lstm-akf\
├─ docs\                                         # 文档目录，放项目背景、结构设计、数据集方案、实验记录
│  ├─ 项目背景.md                                # 说明项目目的、任务背景、应用场景
│  ├─ 模型结构设计.md                            # 说明 AKF + LSTM 结构、输入输出、损失函数
│  ├─ 数据集构建方案.md                          # 说明视频如何转成训练/验证样本
│  ├─ 开发文档.md                                # 记录模块关系、实现约定、开发说明
│  └─ 操作记录.md                                # 记录命令、实验过程、环境操作
│
├─ scripts\                                      # 运行入口，只放“直接执行”的脚本
│  ├─ build_dataset.py                           # 数据集制备入口
│  ├─ train.py                                   # 训练入口
│  └─ validate.py                                # 验证入口
│
├─ lstm_akf\                                     # 核心源码包，真正逻辑都放这里
│  ├─ __init__.py                                # Python 包初始化文件
│  │
│  ├─ datasets\                                  # 数据集相关代码，不放真实数据，只放处理逻辑
│  │  ├─ __init__.py                             # datasets 子包初始化
│  │  ├─ builder.py                              # 数据集构建主流程：从视频到 JSON 样本
│  │  ├─ detection_exporter.py                   # YOLO 检测与目标筛选逻辑
│  │  ├─ track_segment_builder.py                # 连续轨迹切段、滑窗切样本逻辑
│  │  ├─ json_dataset.py                         # 训练/验证时读取 JSON 样本的 Dataset 类
│  │  └─ split.py                                # train / val 划分逻辑
│  │
│  ├─ models\                                    # 模型定义代码
│  │  ├─ __init__.py                             # models 子包初始化
│  │  ├─ akf.py                                  # AKF 模块
│  │  ├─ lstm_akf.py                             # 总模型：AKF 基线 + LSTM 残差预测
│  │  └─ losses.py                               # 损失函数定义
│  │
│  └─ training\                                  # 训练与验证流程代码
│     ├─ __init__.py                             # training 子包初始化
│     ├─ trainer.py                              # 训练循环：epoch、反向传播、优化器更新
│     ├─ validator.py                            # 验证流程：跑验证集、汇总结果
│     ├─ metrics.py                              # 指标计算，比如平均误差、终点误差
│     ├─ checkpoint.py                           # 保存/加载 best.pt、last.pt
│     └─ runner.py                               # 串联训练和验证的总控逻辑
│
├─ data\                                         # 真实数据目录，只放文件，不放处理代码
│  ├─ video\                                     # 原始视频目录
│  └─ dataset\                                   # 生成后的数据集目录
│     ├─ train\                                  # 训练集 JSON 样本
│     └─ val\                                    # 验证集 JSON 样本
│
├─ weights\                                      # 外部预训练权重目录
│  └─ yolo\
│     └─ best.pt                                 # YOLO 检测模型权重
│
├─ outputs\                                      # 实验输出目录，建议按实验号统一归档
│  ├─ exp001\
│  │  ├─ checkpoints\                            # 当前实验的模型权重
│  │  │  ├─ best.pt                              # 当前实验最优权重
│  │  │  └─ last.pt                              # 当前实验最后一次保存的权重
│  │  ├─ train\                                  # 当前实验训练输出
│  │  │  ├─ log.csv                              # 训练日志
│  │  │  └─ curves.png                           # loss 曲线等可视化
│  │  └─ val\                                    # 当前实验验证输出
│  │     ├─ metrics.json                         # 验证指标
│  │     └─ plots\                               # 轨迹对比图、误差图等
│  └─ exp002\                                    # 第二组实验
│
├─ README.md                                     # 项目总说明
└─ .gitignore                                    # 忽略缓存、输出、权重等不必提交的文件
